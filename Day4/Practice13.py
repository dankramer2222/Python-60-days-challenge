# Create a color_codes variable and assign a tuple to it.
#
# The tuple should contain three tuples as items. Each of the three tuples should contain three items.
# The items can be any type.

color_codes = ((1, 2, 3), ('a', 'b', 'c'), ('3.11', '2,28', '99.1'))
print(color_codes)